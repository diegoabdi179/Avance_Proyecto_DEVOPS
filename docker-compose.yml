version: '3.8'
services:
  web-app:
    build: .
    ports:
      - "80:5000"
    volumes:
      - ./logs:/app/logs
    networks:
      - devops-network

networks:
  devops-network:
    driver: bridge
